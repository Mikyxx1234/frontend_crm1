/*
 * Placeholder — a UI real é montada na Fase 6 do plano de
 * reconstrução (id: inbox-page). Mantemos o arquivo aqui para que
 * `/inbox-v2` resolva enquanto a camada de API + adapters + hooks
 * (Fases 3, 4, 5) é construída.
 */
import ClientPage from "./client-page";

export default function Page() {
  return <ClientPage />;
}
